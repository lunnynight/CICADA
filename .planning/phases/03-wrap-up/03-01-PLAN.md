---
phase: 03-wrap-up
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - test/widgets/diagnostic_page_test.dart
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03

must_haves:
  truths:
    - "DiagnosticPage goto_setup triggers onNavigate(1)"
    - "DiagnosticPage goto_models triggers onNavigate(7)"
    - "DiagnosticPage goto_dashboard triggers onNavigate(0)"
  artifacts:
    - path: "test/widgets/diagnostic_page_test.dart"
      provides: "Navigation callback widget tests"
      min_lines: 40
  key_links:
    - from: "test/widgets/diagnostic_page_test.dart"
      to: "lib/pages/diagnostic_page.dart"
      via: "testWidgets with onNavigate callback capture"
      pattern: "onNavigate.*capturedIndex"
---

<objective>
Verify DiagnosticPage navigation callbacks via widget tests.

Purpose: Research confirmed navigation indices (1=setup, 7=models, 0=dashboard) match HomePage._buildPage switch. Per D-01, code is already correct — this plan adds widget tests to prove it. Per D-02, if indices are wrong, fix them (research says they're correct).
Output: test/widgets/diagnostic_page_test.dart with 3 passing navigation tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wrap-up/03-CONTEXT.md
@.planning/phases/03-wrap-up/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit navigation indices and create diagnostic_page widget tests</name>
  <files>test/widgets/diagnostic_page_test.dart, lib/pages/diagnostic_page.dart, lib/pages/home_page.dart</files>
  <read_first>
    - lib/pages/diagnostic_page.dart (lines 370-395 — onNavigate callback usage, find the exact button/tap targets for goto_setup, goto_models, goto_dashboard)
    - lib/pages/home_page.dart (lines 220-245 — _buildPage switch statement to confirm index mapping)
    - test/widgets/home_page_test.dart (existing widget test patterns for reference)
  </read_first>
  <action>
    1. Read diagnostic_page.dart:377-391 to confirm the three navigation calls:
       - goto_setup calls onNavigate(1)
       - goto_models calls onNavigate(7)
       - goto_dashboard calls onNavigate(0)
    2. Read home_page.dart _buildPage switch to verify indices match:
       - case 0: DashboardPage
       - case 1: SetupPage (or SetupPageNew)
       - case 7: ModelsPage
    3. If any index is WRONG per D-02, fix the index value in diagnostic_page.dart.
    4. Create test/widgets/diagnostic_page_test.dart with 3 testWidgets:
       - 'goto_setup triggers onNavigate with index 1': pump DiagnosticPage with onNavigate callback that captures index, find and tap the setup navigation button, assert capturedIndex == 1
       - 'goto_models triggers onNavigate with index 7': same pattern, tap models button, assert capturedIndex == 7
       - 'goto_dashboard triggers onNavigate with index 0': same pattern, tap dashboard button, assert capturedIndex == 0
    5. Use the pattern from 03-RESEARCH.md Code Examples section:
       ```dart
       int? capturedIndex;
       await tester.pumpWidget(MaterialApp(
         home: DiagnosticPage(onNavigate: (i) => capturedIndex = i),
       ));
       ```
    6. DiagnosticPage constructor may require additional parameters — check the constructor signature and provide required params (mock or default values as needed).
    7. The navigation buttons may be inside scrollable content — use `tester.ensureVisible(find.text('...'))` before tapping if needed.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/project/cicada && flutter test test/widgets/diagnostic_page_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - File test/widgets/diagnostic_page_test.dart exists
    - grep -c "testWidgets" test/widgets/diagnostic_page_test.dart returns 3 (or more)
    - grep "capturedIndex" test/widgets/diagnostic_page_test.dart matches at least 3 lines
    - grep "onNavigate" test/widgets/diagnostic_page_test.dart matches at least 3 lines
    - grep "expect(capturedIndex, equals(1))" test/widgets/diagnostic_page_test.dart matches
    - grep "expect(capturedIndex, equals(7))" test/widgets/diagnostic_page_test.dart matches
    - grep "expect(capturedIndex, equals(0))" test/widgets/diagnostic_page_test.dart matches
    - flutter test test/widgets/diagnostic_page_test.dart exits 0 with all tests passing
  </acceptance_criteria>
  <done>3 widget tests pass, each verifying one navigation callback index (1, 7, 0). NAV-01, NAV-02, NAV-03 verified.</done>
</task>

<task type="auto">
  <name>Task 2: Regression check — all existing tests still pass</name>
  <files></files>
  <read_first>
    - test/widgets/diagnostic_page_test.dart (just created — verify it exists)
  </read_first>
  <action>
    Run the full test suite to confirm no regressions from any index fixes (if any were needed).
    Command: `flutter test --no-coverage`
    All 112+ existing tests plus the 3 new navigation tests must pass.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/project/cicada && flutter test --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - flutter test --no-coverage exits 0
    - Output shows "All tests passed" or equivalent
    - Total test count >= 115 (112 existing + 3 new)
  </acceptance_criteria>
  <done>Full test suite passes with no regressions. Navigation tests integrated.</done>
</task>

</tasks>

<verification>
- flutter test test/widgets/diagnostic_page_test.dart — 3 tests pass
- flutter test --no-coverage — all tests pass, no regressions
</verification>

<success_criteria>
- NAV-01: goto_setup triggers onNavigate(1) — verified by widget test
- NAV-02: goto_models triggers onNavigate(7) — verified by widget test
- NAV-03: goto_dashboard triggers onNavigate(0) — verified by widget test
- All existing tests continue to pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-wrap-up/03-01-SUMMARY.md`
</output>
