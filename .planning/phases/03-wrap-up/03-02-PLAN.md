---
phase: 03-wrap-up
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/pages/settings_page.dart
  - lib/pages/settings_dialogs.dart
  - lib/pages/settings_proxy_section.dart
  - lib/pages/skills_page.dart
  - lib/pages/skill_card.dart
autonomous: true
requirements:
  - SPLIT-01
  - SPLIT-02

must_haves:
  truths:
    - "settings_page.dart is under 800 lines after extraction"
    - "settings_dialogs.dart contains the extracted dialog builders"
    - "settings_proxy_section.dart contains the extracted proxy section"
    - "skills_page.dart is under 800 lines after extraction"
    - "skill_card.dart contains SkillCard, Badge, and custom painters"
    - "All existing tests still pass after file splits"
  artifacts:
    - path: "lib/pages/settings_dialogs.dart"
      provides: "Update risk dialog, backup confirm dialog, update banner"
      min_lines: 50
    - path: "lib/pages/settings_proxy_section.dart"
      provides: "Proxy settings section widget"
      min_lines: 80
    - path: "lib/pages/skill_card.dart"
      provides: "SkillCard, Badge, DiagonalStripesPainter, CornerBracketsPainter"
      min_lines: 100
  key_links:
    - from: "lib/pages/settings_page.dart"
      to: "lib/pages/settings_dialogs.dart"
      via: "import and function/widget call"
      pattern: "import.*settings_dialogs"
    - from: "lib/pages/settings_page.dart"
      to: "lib/pages/settings_proxy_section.dart"
      via: "import and widget instantiation"
      pattern: "import.*settings_proxy_section"
    - from: "lib/pages/skills_page.dart"
      to: "lib/pages/skill_card.dart"
      via: "import and widget instantiation"
      pattern: "import.*skill_card"
---

<objective>
Split settings_page.dart (1292 lines) and skills_page.dart (903 lines) into smaller files, each under 800 lines.

Purpose: Per D-03/D-04/D-05, extract dialogs and proxy section from settings_page. Per D-06/D-07, extract SkillCard and painters from skills_page. Reference setup_page.dart split (1112→320) as successful precedent.
Output: 3 new files created, 2 existing files reduced below 800 lines, all tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wrap-up/03-CONTEXT.md
@.planning/phases/03-wrap-up/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Split settings_page.dart — extract dialogs and proxy section</name>
  <files>lib/pages/settings_page.dart, lib/pages/settings_dialogs.dart, lib/pages/settings_proxy_section.dart</files>
  <read_first>
    - lib/pages/settings_page.dart (full file — understand all private state variables, dialog builders at lines 167-464, update banner at 605-699, proxy section at 740-963, and how they access _SettingsPageState)
    - lib/pages/setup/setup_page.dart (or wherever the split precedent lives — understand the pattern used for setup_page split as reference)
  </read_first>
  <action>
    Per D-03: Create lib/pages/settings_dialogs.dart containing:
    - The update risk dialog builder (originally ~lines 167-242, ~63 lines)
    - The backup confirm dialog builder (originally ~lines 243-464, ~221 lines)
    - The update banner builder (originally ~lines 605-699, ~95 lines)
    - Convert private methods (_buildUpdateRiskDialog, _buildBackupConfirmDialog, _buildUpdateBanner) to either:
      (a) Top-level functions accepting required parameters and callbacks, OR
      (b) StatelessWidget classes with constructor parameters
    - All state variables and callbacks these methods reference must become function/constructor parameters

    Per D-04: Create lib/pages/settings_proxy_section.dart containing:
    - The proxy settings section (originally ~lines 740-963, ~224 lines)
    - Convert _buildProxySection to a StatelessWidget (SettingsProxySection) that accepts:
      - ProxyConfig proxyConfig
      - TextEditingController proxyHostCtrl, proxyPortCtrl, proxyUsernameCtrl, proxyPasswordCtrl (keep controllers in parent State, pass them in)
      - ValueChanged<ProxyConfig> onProxyChanged
      - VoidCallback onSave
      - Any other state variables referenced in the proxy section
    - Per Research pitfall #1: Dart private members (_prefix) are file-scoped, so all accessed state must become parameters

    Update lib/pages/settings_page.dart:
    - Add imports for settings_dialogs.dart and settings_proxy_section.dart
    - Replace extracted method bodies with calls to the new functions/widgets
    - Keep all TextEditingControllers and state variables in _SettingsPageState
    - Per D-05: resulting main file should be ~689 lines (must be < 800)

    Verify: `wc -l lib/pages/settings_page.dart` < 800, `dart analyze lib/pages/settings_page.dart lib/pages/settings_dialogs.dart lib/pages/settings_proxy_section.dart` has no errors.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/project/cicada && wc -l lib/pages/settings_page.dart lib/pages/settings_dialogs.dart lib/pages/settings_proxy_section.dart && flutter analyze lib/pages/settings_page.dart lib/pages/settings_dialogs.dart lib/pages/settings_proxy_section.dart 2>/dev/null; flutter test --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - wc -l lib/pages/settings_page.dart shows < 800 lines
    - File lib/pages/settings_dialogs.dart exists and has > 50 lines
    - File lib/pages/settings_proxy_section.dart exists and has > 80 lines
    - grep "import.*settings_dialogs" lib/pages/settings_page.dart matches
    - grep "import.*settings_proxy_section" lib/pages/settings_page.dart matches
    - wc -l lib/pages/settings_dialogs.dart shows < 800 lines
    - wc -l lib/pages/settings_proxy_section.dart shows < 800 lines
    - flutter test --no-coverage exits 0 (no regressions)
  </acceptance_criteria>
  <done>settings_page.dart < 800 lines. settings_dialogs.dart and settings_proxy_section.dart created. All tests pass. SPLIT-01 complete.</done>
</task>

<task type="auto">
  <name>Task 2: Split skills_page.dart — extract SkillCard and painters</name>
  <files>lib/pages/skills_page.dart, lib/pages/skill_card.dart</files>
  <read_first>
    - lib/pages/skills_page.dart (full file — understand _DiagonalStripesPainter at 557-582, _CornerBracketsPainter at 583-616, _SkillCard at 617-877, _Badge at 878-903, and how _SkillCard accesses parent state via closures)
  </read_first>
  <action>
    Per D-06: Create lib/pages/skill_card.dart containing:
    - _DiagonalStripesPainter class (CustomPainter, ~lines 557-582)
    - _CornerBracketsPainter class (CustomPainter, ~lines 583-616)
    - SkillCard widget (rename from _SkillCard to make it public, ~lines 617-877)
      - Convert all closure-captured parent state references to explicit constructor parameters
      - Per Research pitfall #2: Add callback parameters for onInstall, onUninstall, and any other parent-invoked actions
      - Constructor should accept: Skill skill, VoidCallback? onInstall, VoidCallback? onUninstall, plus any other callbacks found during read
    - Badge widget (rename from _Badge to make it public, ~lines 878-903)
    - Make painters public too (remove _ prefix) since they're used by SkillCard in the new file

    Update lib/pages/skills_page.dart:
    - Add import for skill_card.dart
    - Remove the extracted classes
    - Update references from _SkillCard to SkillCard, _Badge to Badge
    - Pass required callbacks when instantiating SkillCard
    - Per D-07: resulting main file should be ~558 lines (must be < 800)

    Verify: `wc -l lib/pages/skills_page.dart` < 800, no analysis errors, all tests pass.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/project/cicada && wc -l lib/pages/skills_page.dart lib/pages/skill_card.dart && flutter test --no-coverage</automated>
  </verify>
  <acceptance_criteria>
    - wc -l lib/pages/skills_page.dart shows < 800 lines
    - File lib/pages/skill_card.dart exists and has > 100 lines
    - wc -l lib/pages/skill_card.dart shows < 800 lines
    - grep "import.*skill_card" lib/pages/skills_page.dart matches
    - grep "class SkillCard" lib/pages/skill_card.dart matches
    - grep "class Badge" lib/pages/skill_card.dart matches (or similar public name)
    - flutter test --no-coverage exits 0 (no regressions)
  </acceptance_criteria>
  <done>skills_page.dart < 800 lines. skill_card.dart created with SkillCard, Badge, and painters. All tests pass. SPLIT-02 complete.</done>
</task>

</tasks>

<verification>
- wc -l lib/pages/settings_page.dart — under 800
- wc -l lib/pages/skills_page.dart — under 800
- wc -l lib/pages/settings_dialogs.dart lib/pages/settings_proxy_section.dart lib/pages/skill_card.dart — all under 800
- flutter test --no-coverage — all tests pass
</verification>

<success_criteria>
- SPLIT-01: settings_page.dart < 800 lines, sub-files < 800 lines, functionality preserved
- SPLIT-02: skills_page.dart < 800 lines, sub-files < 800 lines, functionality preserved
- Zero test regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-wrap-up/03-02-SUMMARY.md`
</output>
