---
phase: 03-wrap-up
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - test/models/dashboard_stats_test.dart
  - test/models/diagnostic_test.dart
  - test/models/mcp_server_test.dart
  - test/models/provider_test.dart
  - test/models/proxy_config_test.dart
  - test/models/skill_test.dart
  - test/core/result_test.dart
  - test/core/app_error_test.dart
  - test/core/json_file_test.dart
  - test/core/platform_info_test.dart
autonomous: true
requirements:
  - COV-01

must_haves:
  truths:
    - "All 6 model classes have fromJson/toJson round-trip tests"
    - "All model copyWith methods are tested for field preservation"
    - "Result<T> Success and Failure paths are tested"
    - "AppError sealed hierarchy subtypes are tested"
    - "JsonFile read/write operations are tested"
    - "PlatformInfo properties return expected values"
  artifacts:
    - path: "test/models/dashboard_stats_test.dart"
      provides: "DashboardStats, RecentSession, AttentionItem tests"
      min_lines: 30
    - path: "test/models/mcp_server_test.dart"
      provides: "McpServer, McpPreset fromJson/toJson/copyWith tests"
      min_lines: 40
    - path: "test/models/proxy_config_test.dart"
      provides: "ProxyConfig fromJson/toJson/isConfigured tests"
      min_lines: 30
    - path: "test/models/skill_test.dart"
      provides: "Skill fromJson/toJson tests"
      min_lines: 30
    - path: "test/core/result_test.dart"
      provides: "Result Success/Failure map/flatMap/onSuccess/onFailure tests"
      min_lines: 40
    - path: "test/core/app_error_test.dart"
      provides: "AppError subtype construction and message tests"
      min_lines: 30
  key_links:
    - from: "test/models/mcp_server_test.dart"
      to: "lib/models/mcp_server.dart"
      via: "import and direct class instantiation"
      pattern: "McpServer.fromJson"
    - from: "test/core/result_test.dart"
      to: "lib/core/result.dart"
      via: "import and pattern matching"
      pattern: "Success|Failure"
---

<objective>
Create unit tests for all 6 model files and 4 core files to build coverage foundation.

Purpose: Per D-08, bottom-up strategy starts with models and core — pure Dart classes with no Flutter dependency, easiest to test, highest coverage-per-effort ratio. Per D-10, this is the first step toward 80% coverage.
Output: 10 new test files covering models/ and core/ layers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wrap-up/03-CONTEXT.md
@.planning/phases/03-wrap-up/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for all 6 model files</name>
  <files>test/models/dashboard_stats_test.dart, test/models/diagnostic_test.dart, test/models/mcp_server_test.dart, test/models/provider_test.dart, test/models/proxy_config_test.dart, test/models/skill_test.dart</files>
  <read_first>
    - lib/models/dashboard_stats.dart (68 lines — DashboardStats, RecentSession, AttentionItem classes, their constructors, fromJson, toJson, copyWith)
    - lib/models/diagnostic.dart (58 lines — DiagnosticReport, TokenRecord, their fields and serialization)
    - lib/models/mcp_server.dart (145 lines — McpServer, McpPreset, McpTransport enum, fromJson/toJson/copyWith)
    - lib/models/provider.dart (55 lines — ProviderConfig, ModelInfo, their fields and serialization)
    - lib/models/proxy_config.dart (102 lines — ProxyConfig, ProxyType enum, fromJson/toJson/copyWith/isConfigured)
    - lib/models/skill.dart (128 lines — Skill class, its fields, fromJson/toJson)
  </read_first>
  <action>
    Create test/models/ directory if it doesn't exist.

    For EACH of the 6 model files, create a corresponding test file using `package:test/test.dart` (pure Dart, no Flutter dependency needed):

    1. test/models/dashboard_stats_test.dart:
       - Test DashboardStats.fromJson with valid JSON → verify all fields
       - Test DashboardStats.toJson round-trip (fromJson then toJson equals original)
       - Test RecentSession.fromJson/toJson round-trip
       - Test AttentionItem.fromJson/toJson round-trip
       - If copyWith exists, test it preserves unchanged fields (per STATE.md: use mutable map, not const)

    2. test/models/diagnostic_test.dart:
       - Test DiagnosticReport construction and field access
       - Test TokenRecord.fromJson/toJson round-trip
       - Test any factory constructors or named constructors

    3. test/models/mcp_server_test.dart:
       - Test McpServer.fromJson with valid JSON including all fields
       - Test McpServer.toJson round-trip
       - Test McpServer.copyWith preserves unchanged fields and updates target field
       - Test McpPreset.fromJson/toJson round-trip
       - Test McpTransport enum serialization (stdio, sse)

    4. test/models/provider_test.dart:
       - Test ProviderConfig.fromJson/toJson round-trip
       - Test ModelInfo.fromJson/toJson round-trip
       - Test edge cases (empty lists, null optional fields)

    5. test/models/proxy_config_test.dart:
       - Test ProxyConfig.fromJson/toJson round-trip
       - Test ProxyConfig.copyWith preserves unchanged fields
       - Test ProxyConfig.isConfigured returns true when type=ProxyType.http and host is set (per STATE.md known issue)
       - Test ProxyConfig.isConfigured returns false for default/empty config
       - Test ProxyType enum values

    6. test/models/skill_test.dart:
       - Test Skill.fromJson with valid JSON → verify all fields
       - Test Skill.toJson round-trip
       - Test edge cases (optional fields missing)

    Import pattern: `import 'package:test/test.dart';` and relative import to model file like `import '../../lib/models/mcp_server.dart';`
    
    NOTE: Check actual import paths — if tests use `package:cicada/models/...` pattern, follow that instead.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/project/cicada && flutter test test/models/</automated>
  </verify>
  <acceptance_criteria>
    - Directory test/models/ exists with 6 test files
    - grep -l "fromJson" test/models/*_test.dart returns all 6 files
    - grep -l "toJson" test/models/*_test.dart returns at least 5 files
    - grep -c "test(" test/models/mcp_server_test.dart returns >= 4
    - grep -c "test(" test/models/proxy_config_test.dart returns >= 4
    - flutter test test/models/ exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 6 model files have unit tests covering fromJson/toJson round-trips, copyWith, and edge cases. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for all 4 core files</name>
  <files>test/core/result_test.dart, test/core/app_error_test.dart, test/core/json_file_test.dart, test/core/platform_info_test.dart</files>
  <read_first>
    - lib/core/result.dart (66 lines — sealed class Result<T>, Success<T>, Failure<T>, map, flatMap, onSuccess, onFailure methods)
    - lib/core/app_error.dart (184 lines — sealed class AppError, ConfigError, NetworkError, AuthError, InstallError, ServiceError, ValidationError with named constructors)
    - lib/core/json_file.dart (50 lines — JsonFile class for atomic JSON read/write)
    - lib/core/platform/platform_info.dart (17 lines — PlatformInfo with isDesktop, isMobile, etc.)
    - lib/core/platform/shell_env.dart (58 lines — ShellEnv for macOS PATH resolution, may be hard to test on Windows)
  </read_first>
  <action>
    Create test/core/ directory if it doesn't exist.

    1. test/core/result_test.dart:
       - Test Success<int>(42).map((v) => v * 2) returns Success(84)
       - Test Failure<int>(someError).map(...) returns Failure unchanged
       - Test Success.flatMap returning Failure
       - Test Failure.flatMap is not called
       - Test onSuccess callback is invoked for Success
       - Test onFailure callback is invoked for Failure
       - Test pattern matching: switch(result) { case Success(value: v) => ..., case Failure(...) => ... }

    2. test/core/app_error_test.dart:
       - Test each AppError subtype can be constructed via named constructors:
         - ConfigError.readFailed(), ConfigError.writeFailed(), ConfigError.parseFailed(), ConfigError.notFound()
         - NetworkError.timeout(), NetworkError.unreachable(), NetworkError.proxyFailed()
         - AuthError.invalidKey(), AuthError.expired(), AuthError.rateLimited()
         - InstallError.nodeMissing(), InstallError.openclawFailed(), InstallError.extractFailed(), InstallError.permissionDenied()
         - ServiceError.notRunning(), ServiceError.startFailed(), ServiceError.connectionLost()
         - ValidationError.emptyField(), ValidationError.invalidFormat(), ValidationError.invalidUrl()
       - Verify each has a non-empty message (Chinese string)
       - Verify type checking: ConfigError is AppError, etc.

    3. test/core/json_file_test.dart:
       - Test JsonFile.read with a temp file containing valid JSON → returns parsed map
       - Test JsonFile.read with non-existent file → returns appropriate error/default
       - Test JsonFile.write creates file with correct JSON content
       - Test round-trip: write then read returns same data
       - Use dart:io Directory.systemTemp for temp file paths

    4. test/core/platform_info_test.dart:
       - Test PlatformInfo.isDesktop returns true on Windows (test environment)
       - Test PlatformInfo.isMobile returns false on Windows
       - Test other boolean properties exist and return bool values
       - Note: ShellEnv is macOS-specific, skip on Windows with a comment explaining why

    Import pattern: follow existing test conventions in the project.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/project/cicada && flutter test test/core/</automated>
  </verify>
  <acceptance_criteria>
    - Directory test/core/ exists with 4 test files (result_test.dart, app_error_test.dart, json_file_test.dart, platform_info_test.dart)
    - grep -c "test(" test/core/result_test.dart returns >= 5
    - grep -c "test(" test/core/app_error_test.dart returns >= 6
    - grep "Success" test/core/result_test.dart matches
    - grep "Failure" test/core/result_test.dart matches
    - grep "ConfigError" test/core/app_error_test.dart matches
    - flutter test test/core/ exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 4 core files have unit tests. Result sealed class fully tested. AppError hierarchy tested. JsonFile I/O tested. PlatformInfo tested. All pass.</done>
</task>

</tasks>

<verification>
- flutter test test/models/ — all model tests pass
- flutter test test/core/ — all core tests pass
- flutter test --no-coverage — full suite passes, no regressions
- Per D-09: run flutter test --coverage after this plan to measure progress
</verification>

<success_criteria>
- 10 new test files created (6 models + 4 core)
- All new tests pass
- Coverage measurably increased from 10.6% baseline
- No regressions in existing 112+ tests
</success_criteria>

<output>
After completion, create `.planning/phases/03-wrap-up/03-03-SUMMARY.md`
</output>
