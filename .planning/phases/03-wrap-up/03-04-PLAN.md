---
phase: 03-wrap-up
plan: 04
type: execute
wave: 2
depends_on:
  - 03-03
files_modified:
  - test/data/config_repository_test.dart
  - test/data/mcp_directory_test.dart
  - test/data/mcp_presets_test.dart
  - test/data/clawhub_catalog_test.dart
  - test/services/bundled_installer_service_test.dart
  - test/services/bundled_skill_service_test.dart
  - test/services/claude_code_service_test.dart
  - test/services/preset_service_test.dart
autonomous: true
requirements:
  - COV-01

must_haves:
  truths:
    - "All 4 data layer classes have unit tests"
    - "ConfigRepository CRUD operations are tested with temp files"
    - "BundledInstallerService, BundledSkillService, ClaudeCodeService, PresetService have tests"
    - "Coverage has increased significantly toward 80% target"
  artifacts:
    - path: "test/data/config_repository_test.dart"
      provides: "ConfigRepository read/write/update tests"
      min_lines: 40
    - path: "test/data/mcp_directory_test.dart"
      provides: "McpDirectory catalog loading tests"
      min_lines: 20
    - path: "test/data/mcp_presets_test.dart"
      provides: "McpPresets template loading tests"
      min_lines: 20
    - path: "test/data/clawhub_catalog_test.dart"
      provides: "ClawHubCatalog skill catalog tests"
      min_lines: 20
    - path: "test/services/bundled_installer_service_test.dart"
      provides: "BundledInstallerService offline install tests"
      min_lines: 40
    - path: "test/services/claude_code_service_test.dart"
      provides: "ClaudeCodeService CLI interaction tests"
      min_lines: 30
  key_links:
    - from: "test/data/config_repository_test.dart"
      to: "lib/data/config_repository.dart"
      via: "import and instance method calls returning Result<T>"
      pattern: "ConfigRepository.*readConfig|writeConfig"
    - from: "test/services/bundled_installer_service_test.dart"
      to: "lib/services/bundled_installer_service.dart"
      via: "import and static method calls with mocked dependencies"
      pattern: "BundledInstallerService"
---

<objective>
Create unit tests for all 4 data layer files and 4 remaining untested service files.

Purpose: Per D-08, continue bottom-up: data layer after models/core, then remaining services. Per D-09, measure coverage after completion. These 8 files represent ~1772 lines of untested production code.
Output: 8 new test files covering data/ and remaining services/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wrap-up/03-CONTEXT.md
@.planning/phases/03-wrap-up/03-RESEARCH.md
@.planning/phases/03-wrap-up/03-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create unit tests for all 4 data layer files</name>
  <files>test/data/config_repository_test.dart, test/data/mcp_directory_test.dart, test/data/mcp_presets_test.dart, test/data/clawhub_catalog_test.dart</files>
  <read_first>
    - lib/data/config_repository.dart (137 lines — ConfigRepository instance class using Result<T>, readConfig, writeConfig, updateConfig methods, depends on JsonFile and PlatformInfo for config dir)
    - lib/data/mcp_directory.dart (167 lines — McpDirectory, curated MCP catalog, likely reads from bundled JSON assets)
    - lib/data/mcp_presets.dart (89 lines — McpPresets, preset templates, likely reads from bundled JSON)
    - lib/data/clawhub_catalog.dart (187 lines — ClawHubCatalog, skill catalog, likely HTTP or asset-based)
    - lib/core/result.dart (already tested in Plan 03 — understand Success/Failure pattern for asserting data layer returns)
    - test/services/config_service_test.dart (existing test — reference for mock patterns and test conventions used in this project)
  </read_first>
  <action>
    Create test/data/ directory if it doesn't exist.

    1. test/data/config_repository_test.dart:
       - ConfigRepository uses instance methods returning Result<T>
       - Create a temp directory for test isolation (Directory.systemTemp.createTempSync)
       - Test readConfig on non-existent file → returns Failure or default
       - Test writeConfig creates file with correct JSON
       - Test readConfig after writeConfig → returns Success with written data
       - Test updateConfig modifies specific keys while preserving others
       - Clean up temp files in tearDown
       - May need to mock or override the config directory path — check constructor

    2. test/data/mcp_directory_test.dart:
       - Read the class to understand if it loads from bundled assets or filesystem
       - If asset-based: test the parsing logic with sample JSON data
       - If filesystem-based: use temp files
       - Test that catalog entries have expected structure (name, command, etc.)
       - Test empty/malformed input handling

    3. test/data/mcp_presets_test.dart:
       - Similar approach to mcp_directory
       - Test preset loading and structure validation
       - Test that presets contain expected fields

    4. test/data/clawhub_catalog_test.dart:
       - Read the class to understand data source (HTTP vs asset)
       - If HTTP-based: use mock_http_client.dart from test/services/helpers/
       - If asset-based: test parsing logic with sample data
       - Test catalog entry structure
       - Test error handling for malformed data

    Use existing test helpers: mock_http_client.dart and fake_process_runner.dart from test/services/helpers/ where applicable.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/project/cicada && flutter test test/data/</automated>
  </verify>
  <acceptance_criteria>
    - Directory test/data/ exists with 4 test files
    - grep -c "test(" test/data/config_repository_test.dart returns >= 3
    - grep "Result\|Success\|Failure" test/data/config_repository_test.dart matches at least 1 line
    - flutter test test/data/ exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 4 data layer files have unit tests. ConfigRepository CRUD tested with temp files. Catalog/preset parsing tested. All pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create unit tests for 4 remaining untested service files</name>
  <files>test/services/bundled_installer_service_test.dart, test/services/bundled_skill_service_test.dart, test/services/claude_code_service_test.dart, test/services/preset_service_test.dart</files>
  <read_first>
    - lib/services/bundled_installer_service.dart (413 lines — static methods for offline Node.js/OpenClaw installation from bundled assets, likely uses Process.run and file I/O)
    - lib/services/bundled_skill_service.dart (148 lines — static methods for bundled skill management, reads from assets)
    - lib/services/claude_code_service.dart (352 lines — static methods for Claude Code CLI interaction, uses Process.run)
    - lib/services/preset_service.dart (19 lines — small, likely loads preset data)
    - test/services/helpers/mock_http_client.dart (mock HTTP client pattern)
    - test/services/helpers/fake_process_runner.dart (fake process runner pattern for CLI mocking)
    - test/services/installer_service_test.dart (existing test — reference for how InstallerService was tested, similar patterns apply to BundledInstallerService)
  </read_first>
  <action>
    For each untested service, create a test file following Phase 1 established patterns:

    1. test/services/bundled_installer_service_test.dart:
       - BundledInstallerService has static methods that shell out to processes and do file I/O
       - Use FakeProcessRunner (from helpers/) to mock Process.run calls
       - Test key methods: check if bundled assets exist, extract bundled Node.js, install OpenClaw from bundle
       - Test error paths: missing assets, extraction failure, permission errors
       - Mock file system operations where needed (use temp directories)

    2. test/services/bundled_skill_service_test.dart:
       - Test skill discovery from bundled assets
       - Test skill metadata parsing
       - Test handling of missing/corrupt bundled skills
       - May need to set up temp asset directory structure

    3. test/services/claude_code_service_test.dart:
       - Static methods that invoke `claude` CLI
       - Use FakeProcessRunner to mock CLI calls
       - Test: detect Claude Code installation (mock process returning version string)
       - Test: detect Claude Code not installed (mock process throwing/returning error)
       - Test: configuration reading/writing via CLI
       - Test error handling for CLI failures

    4. test/services/preset_service_test.dart:
       - Small file (19 lines) — likely a simple loader
       - Test preset loading returns expected data structure
       - Test error handling

    Follow existing conventions from Phase 1 service tests. Use `group()` for organizing related tests. Use descriptive test names in English.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/project/cicada && flutter test test/services/bundled_installer_service_test.dart test/services/bundled_skill_service_test.dart test/services/claude_code_service_test.dart test/services/preset_service_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - File test/services/bundled_installer_service_test.dart exists with >= 3 test cases
    - File test/services/bundled_skill_service_test.dart exists with >= 2 test cases
    - File test/services/claude_code_service_test.dart exists with >= 3 test cases
    - File test/services/preset_service_test.dart exists with >= 1 test case
    - flutter test on all 4 files exits 0 with all tests passing
    - No regressions: flutter test --no-coverage exits 0
  </acceptance_criteria>
  <done>All 4 remaining untested services have test files. BundledInstallerService, BundledSkillService, ClaudeCodeService, PresetService covered. All tests pass.</done>
</task>

</tasks>

<verification>
- flutter test test/data/ — all data tests pass
- flutter test test/services/ — all service tests pass (including existing ones)
- flutter test --no-coverage — full suite passes
- Per D-09: run flutter test --coverage to measure progress toward 80%
</verification>

<success_criteria>
- 8 new test files created (4 data + 4 services)
- All new tests pass
- No regressions in existing tests
- Coverage significantly closer to 80% target
</success_criteria>

<output>
After completion, create `.planning/phases/03-wrap-up/03-04-SUMMARY.md`
</output>
