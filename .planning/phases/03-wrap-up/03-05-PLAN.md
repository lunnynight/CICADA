---
phase: 03-wrap-up
plan: 05
type: execute
wave: 3
depends_on:
  - 03-02
  - 03-03
  - 03-04
files_modified:
  - test/providers/config_provider_test.dart
autonomous: true
requirements:
  - COV-01

must_haves:
  truths:
    - "flutter test --coverage reports >= 80% overall coverage"
    - "All existing tests plus all new tests pass"
    - "Coverage gaps identified and filled to reach 80% threshold"
  artifacts:
    - path: "coverage/lcov.info"
      provides: "Coverage report showing >= 80%"
  key_links:
    - from: "flutter test --coverage"
      to: "coverage/lcov.info"
      via: "flutter test coverage output"
      pattern: "coverage"
---

<objective>
Measure coverage after Plans 02-04, identify gaps, and fill them to reach the 80% target.

Purpose: Per D-09, measure after each plan and adjust. This plan runs after all test-writing and file-splitting plans complete. It measures actual coverage, identifies the biggest remaining gaps, and writes targeted tests to close them. Per D-10, the target is >= 80%.
Output: Coverage report >= 80%, any gap-fill test files created.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-wrap-up/03-CONTEXT.md
@.planning/phases/03-wrap-up/03-RESEARCH.md
@.planning/phases/03-wrap-up/03-03-SUMMARY.md
@.planning/phases/03-wrap-up/03-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure coverage and identify gaps</name>
  <files></files>
  <read_first>
    - .planning/phases/03-wrap-up/03-03-SUMMARY.md (what models/core tests were created)
    - .planning/phases/03-wrap-up/03-04-SUMMARY.md (what data/services tests were created)
  </read_first>
  <action>
    1. Run `flutter test --coverage` to generate coverage/lcov.info
    2. Parse lcov.info to calculate overall coverage percentage:
       ```bash
       # Sum hit and total lines from lcov.info
       awk -F: '/^LH:/{hit+=$2} /^LF:/{total+=$2} END{printf "Coverage: %.1f%% (%d/%d)\n", hit/total*100, hit, total}' coverage/lcov.info
       ```
    3. If coverage >= 80%: Task is done. Record the number and proceed to Task 2 for final verification.
    4. If coverage < 80%: Identify the files with lowest coverage that have the most uncovered lines:
       ```bash
       # Parse lcov.info to find files with most uncovered lines
       # Focus on lib/ files, sorted by uncovered line count descending
       ```
       List the top 5-10 files with most uncovered lines. These are the gap-fill targets.
    5. Record findings: current coverage %, top gap files, estimated tests needed.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/project/cicada && flutter test --coverage && awk -F: '/^LH:/{hit+=$2} /^LF:/{total+=$2} END{printf "Coverage: %.1f%% (%d/%d)\n", hit/total*100, hit, total}' coverage/lcov.info</automated>
  </verify>
  <acceptance_criteria>
    - coverage/lcov.info file exists after flutter test --coverage
    - awk command outputs a coverage percentage
    - All tests pass (flutter test --coverage exits 0)
  </acceptance_criteria>
  <done>Coverage measured. Gap files identified if below 80%.</done>
</task>

<task type="auto">
  <name>Task 2: Fill coverage gaps to reach 80% and final verification</name>
  <files>test/providers/config_provider_test.dart</files>
  <read_first>
    - coverage/lcov.info (parse to find which files have lowest coverage)
    - The top 3-5 uncovered files identified in Task 1 (read their source to understand what needs testing)
  </read_first>
  <action>
    **If already >= 80% from Task 1:** Skip gap-fill, proceed directly to final verification below.

    **If below 80%:** Write targeted tests for the files with the most uncovered lines. Priority order per D-08:
    
    1. config_provider.dart (42 lines, providers layer — test Riverpod providers with ProviderScope + overrideWith pattern from Phase 2):
       - Create test/providers/config_provider_test.dart
       - Test configRepositoryProvider returns a ConfigRepository instance
       - Test configDataProvider returns config data
       - Test configuredProvidersProvider, mcpServersProvider, proxyConfigProvider
       - Use ProviderScope with overrides to inject test data

    2. Any remaining service methods not covered by existing tests:
       - Read the specific service test files from Phase 1 to see which methods are tested
       - Add tests for untested public methods in existing test files
       - Focus on methods with the most lines (highest coverage impact)

    3. Any utility files (lib/utils/, lib/app/) not yet covered:
       - Check if key_masker.dart or other utils have tests
       - Add simple unit tests for pure functions

    **Strategy:** Per D-09, after each batch of new tests, re-run `flutter test --coverage` and check progress. Stop when >= 80%.

    **Final verification (always run):**
    ```bash
    flutter test --coverage
    # Verify >= 80%
    awk -F: '/^LH:/{hit+=$2} /^LF:/{total+=$2} END{printf "Coverage: %.1f%% (%d/%d)\n", hit/total*100, hit, total}' coverage/lcov.info
    # Verify all tests pass
    flutter test --no-coverage
    ```
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/project/cicada && flutter test --coverage && awk -F: '/^LH:/{hit+=$2} /^LF:/{total+=$2} END{if(hit/total*100 >= 80) print "PASS: " hit/total*100 "%"; else print "FAIL: " hit/total*100 "%"}' coverage/lcov.info</automated>
  </verify>
  <acceptance_criteria>
    - flutter test --coverage exits 0 (all tests pass)
    - awk output shows "PASS:" with coverage >= 80%
    - No test regressions (total test count >= previous count)
  </acceptance_criteria>
  <done>Overall test coverage >= 80%. All tests pass. COV-01 complete.</done>
</task>

</tasks>

<verification>
- flutter test --coverage exits 0
- Coverage percentage >= 80% as reported by lcov.info parsing
- All tests pass with no regressions
- No files > 800 lines (verified by Plan 02)
- Navigation tests pass (verified by Plan 01)
</verification>

<success_criteria>
- COV-01: flutter test --coverage reports >= 80% overall coverage
- All Phase 1, 2, and 3 tests pass together
- Zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-wrap-up/03-05-SUMMARY.md`
</output>
